extends layout

block content
  #nav
    img(src='/images/logo.png')
    if user
      h1 #{user.email}
      a.login(href="/logout") Logout
    else
      h1 Lab Manager Tools
      a.login(href="/auth/basecamp")
        img(src="/images/basecamp.png",width=32) 
        span Sign in
  if user
    #cal(user=user.email)